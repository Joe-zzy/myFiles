20200224：
创建程序基本框架
20200225：
1、完成Login程序

20200312：
这个程序，数据库使用2个表，分别是称重前结果、称重后结果，没有数据合并的表。
主界面显示的数据，通过合并截取这两个表得到。
不在创建第三个表：合并表，考虑到耗时的问题。
但也要确认好风险，确保这种形式的存储和显示方式不会有问题！！

20200323：
写入数据的类，在执行数据库写入操作后，判断是否写入成功。如果不成功，重写一次。如果还是不成功，报警。

20200413：
1、修改PLC重连部分
2、修改产品序号显示方式
3、修改程序启动时加载数据方式：白班只加载白班的数据，晚班只加载晚班的数据
4、后称重数据匹配前称重时，不仅需要比对条码，还需要比对时间  -- 在数据库、前界面显示 上的应用
5、同时读取PLC对个变量时，采用读取多变量的方式，而不再是一个一个的读取

20200414：
1、主要的流程、方法块添加 try . . . catch . . .
2、点检界面，还是需要添加取消按钮，点检完成或是失败，给PLC反馈信号